syntax = "proto3";

option go_package = "STQ/protos";

package stq;

import "types.proto";

service Access {
  rpc Echo(Empty) returns (EchoRes);
  rpc Stop(Empty) returns (EchoRes);
}

message EchoRes {
}
