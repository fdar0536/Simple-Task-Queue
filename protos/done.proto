syntax = "proto3";

option go_package = "STQ/protos";

package stq;

import "types.proto";

service Done {
  rpc List(QueueReq) returns (stream ListTaskRes);
  rpc Details(TaskDetailsReq) returns (TaskDetailsRes);
  rpc Clear(QueueReq) returns (Empty);
}
